//WAP to find the factorial of agiven umber using reccursion.
#include<stdio.h>
#include<conio.h>
int factorial(int)
int main()
{
int n,f;
printf("Enter a number");
scanf("%d",&n);
f=factorial(n);
printf("Factorial of number is %d",f);
	getch();
	return 0;
}
int factorial(int x)
{
	int fact=1,i;
	fact=fact*i;
	return fact;
}

